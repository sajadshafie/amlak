import React from "react";
import { Grid } from "@mui/material";
import Main from "@/Layout/main";
const advertisement = (): JSX.Element => {
  return <Main active={1}></Main>;
};

export default advertisement;
