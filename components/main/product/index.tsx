import { Grid, Typography } from "@mui/material";
import React from "react";

const Product: React.FC = () => {
  return (
    <Grid>
      <Typography variant="h3">املاک</Typography>
    </Grid>
  );
};

export default Product;
