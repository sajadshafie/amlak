export type userTypes = {
  username: string;
  family: string;
  role: string;
  department: string;
};

export interface contextType {
  boxShadow: string;
  save_product: boolean;
  maxWidth: object;
  px: object;
  margin: string;
  userDetail: userTypes;
}
